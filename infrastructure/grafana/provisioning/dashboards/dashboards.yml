apiVersion: 1
providers:
  - name: PayFlow
    orgId: 1
    folder: PayFlow
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
