# dependencies
node_modules/

# build output (regenerated by `npm run build` / Vercel — must not be committed)
dist/

# local env files (never commit real secrets/URLs)
.env
.env.local
.env.*.local

# misc
*.log
.DS_Store
tsconfig.tsbuildinfo
